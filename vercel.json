{
  "buildCommand": "cd web_dashboard && npm install && npm run build",
  "outputDirectory": "web_dashboard/dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
